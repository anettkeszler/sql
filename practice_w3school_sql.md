# W3School exercises

#### SQL Syntax
- Table in a database: A structured set of data organized in rows and columns

- SQL keywords are not case-insensitive.

#### SQL Select
#### SQL SELECT DISTINCT
 - DISTINCT keyword: to return unique values from a column that contains duplicates

#### SQL WHERE
- WHERE keyword: to filter records that meet a specified condition

#### SQL ORDER BY
- ORDER BY keyword: to sort records in ascending or descending order
